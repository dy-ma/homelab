# Media stack

Planned home for the Servarr stack, download clients, Seerr, and supporting
services. Native Jellyfin host configuration belongs under `hosts/dylmini/`.

The eventual `compose.yaml` will live in this directory. Persistent application
state and the media library will be mounted from locations outside this Git
repository and documented here once chosen.
