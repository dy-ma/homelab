[alias]
	s = status
	aa = add --all
	cm = commit -m

[core]
	pager = delta

[interactive]
	diffFilter = delta --color-only

[delta]
	dark = true
	line-numbers = true
	navigate = true
	syntax-theme = gruvbox-dark

[diff]
	colorMoved = default

[merge]
	conflictStyle = zdiff3
